<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:color="#FFFFFF" android:state_checked="true"/>
    <!-- Pressed state -->
    <item android:color="#FFFFFF" android:state_pressed="true"/>
    <!-- Normal state -->
    <item android:color="#51A8A6"/>
</selector>
